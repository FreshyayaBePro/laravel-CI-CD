#Définir un nom
name : Laravel CI
#Définir les triggers
on : 
  push :
    branches : [main]
jobs :
  build:
    runs-on : ubuntu-latest
    steps : 
        - name : récupérer le code source
          uses : actions/checkout@v5

          #2 - exécuter les tests 
        - name : Run test
          run : cd app_laravel && composer install && cp .env .en.example && php artsan key:gen && php artisan test 


